import React from 'react';
import AdBase from './ad-base';

const AdCodeBlock = () => {
  return (
    <AdBase
      id={`shadcnui-expansions-code-block-${Math.floor(Math.random() * 1000)}`}
      slot="9926290972"
      className="mt-4"
    />
  );
};

export default AdCodeBlock;
